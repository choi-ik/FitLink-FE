import { notificationMap } from "../../_constants";

export const NOTIFICATION_TYPE = notificationMap["reservation-cancel"].type;
export const NOTIFICATION_QUERY_TYPE = notificationMap["reservation-cancel"].queryType;
